// react framework

import { Button, Modal } from "@mantine/core";

type PropsType = {
  client_sdr_id: number;
};

export default function CreateBumpInstructionModal(props: PropsType) {
  return <>Create bump instruction</>;
}
